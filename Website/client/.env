# Production (DigitalOcean App Platform)
# Update these values in the App Platform environment variables for production
# Point React frontend to your backend API
REACT_APP_API_URL=https://wrapntrack-backend-xyz.ondigitalocean.app
    
# Local development (uncomment and set during local dev only)
# To run locally, set these in your local environment. Production should use the DO hosts above.